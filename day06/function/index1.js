// 화살표 함수
const a = (x) => {
  return `${x}님 안녕하세요!`;
};

const b = a("엄준식");
console.log(b);

// x, y 넣으면 제곱하는 화살표 함수
const square = (x, y) => {
  return x ** y;
};

// x 넣으면 럭키비키잖아
const wonyoung = (x) => {
  return `${x}라서 럭키비키잖아😉`;
};
